# SPDX-FileCopyrightText: 2023 Zextras <https://www.zextras.com>
#
# SPDX-License-Identifier: AGPL-3.0-only

" THIS FILE IS AUTOGENERATED BY GRAPHQL-CODEGEN. DO NOT EDIT! "
scalar DateTime

type File implements Node {
	created_at: DateTime!
	extension: String
	id: ID!
	mime_type: String!
	name: String!
	size: Float!
	type: NodeType!
	updated_at: DateTime!
}

type Folder implements Node {
	created_at: DateTime!
	id: ID!
	name: String!
	type: NodeType!
	updated_at: DateTime!
}

interface Node {
	created_at: DateTime!
	id: ID!
	name: String!
	type: NodeType!
	updated_at: DateTime!
}

type NodePage {
	nodes: [Node]!
	page_token: String
}

enum NodeType {
	APPLICATION
	AUDIO
	FOLDER
	IMAGE
	MESSAGE
	OTHER
	PRESENTATION
	SPREADSHEET
	TEXT
	VIDEO
}

type Query {
	findNodes(folder_id: ID!, limit: Int, node_link_id: String, page_token: String): NodePage
	getPublicNode(node_link_id: String!): Node
}
